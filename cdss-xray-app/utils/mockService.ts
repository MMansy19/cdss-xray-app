// This file is no longer used as the application now fully integrates with the backend
// Keeping this file as a reference but it's no longer used in the application
// All functionality now uses the real API endpoints

import { AnalysisResult, PatientVitals } from '../types';

// This function is no longer used and should not be called
export const isDemoMode = async (): Promise<boolean> => {
  return false;
};

// This function is no longer used and should not be called
export const isDemoModeSync = (): boolean => {
  return false;
};
